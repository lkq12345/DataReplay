# 数据回放客户端软件 — 设计方案

## 一、项目现状

```
d:/QTProject/DataReplay/
├── 项目需求.txt
├── 设计方案.md
├── bin/
│   ├── APP_DataReplay.exe        # 已编译的主程序
│   └── libnats.dll               # NATS C库
├── config/
│   └── NATS/
│       └── NatsConfig.xml        # NATS配置(IP=127.0.0.1, 主题:NATSMSG/SendToB/SendToA)
├── dataFiles/
│   ├── 想定-海上编队巡逻想定/
│   │   ├── 想定.xml              # XML格式想定文件
│   │   ├── mapping.json          # 实体ID映射配置
│   │   └── 回放数据/
│   │       └── *_data*.txt       # 每行: 时间戳 JSON
│   ├── 想定-海上编队巡逻想定 - 副本/
│   └── 想定-海上编队巡逻想定222/
├── include/
│   ├── NATS/                     # NATS C库头文件
│   ├── Service_Communication_Factory.h  # 通信工厂抽象基类
│   └── publicDefineAndStruct.h         # 公共定义
├── src/
│   ├── DataReplay.pro            # subdirs 根项目
│   ├── APP_DataReplay/           # 主界面应用
│   │   ├── main.cpp
│   │   ├── DataReplayWidget.h/cpp/ui
│   │   └── ScenarioFilterProxyModel.h/cpp
│   └── Server_DataReplay/        # 动态库
│       ├── Server_DataReplay.h/cpp       # Facade 统一入口（新）
│       ├── ScenarioMgr.h/cpp             # 想定管理
│       ├── DataFileReader.h/cpp          # 大文件读取
│       ├── ReplayEngine.h/cpp            # 回放引擎
│       ├── LogService.h/cpp              # 日志服务
│       └── Communication/
│           ├── NATSClient.h/cpp          # NATS C库面向对象封装
│           ├── Communication_NATS.h/cpp  # NATS通信单例
│           └── Communication_Interior.h/cpp # 进程内消息分发
└── temp/                          # 编译中间文件
```

**现有可复用资产**：
- `NATSClient` — nats.c 的 Qt 面向对象封装（初始化、连接、订阅、发布、销毁）
- `Communication_NATS` — NATS 通信单例（XML 配置读取、发布、订阅回调、断线重连）
- `Communication_Interior` — 进程内部消息分发机制（未来扩展用）
- `Service_Communication_Factory` — 通信工厂抽象基类

## 二、架构设计

### 2.1 前后端分离架构（Facade 模式）

```
┌─────────────────────────────────────────────────────────────────┐
│                    APP_DataReplay (exe)                         │
│                      前端 — UI层 + 控制编排                       │
│                                                                  │
│  职责: 界面展示、用户交互、调用后端接口、显示状态                  │
│  依赖: 只链接 Server_DataReplay.dll                             │
│        只 include "Server_DataReplay.h"（仅此一个头文件）         │
│                                                                  │
│  ┌──────────────┐  ┌────────────┐  ┌──────────┐  ┌───────────┐  │
│  │ 树形文件管理   │  │ 实体配置表  │  │ 导调控制  │  │ 日志显示  │  │
│  │ QTreeView    │  │ QTableView│  │ 按钮+进度 │  │ QTextEdit │  │
│  └──────┬───────┘  └────────────┘  └─────┬────┘  └───────────┘  │
│         │                                │                       │
│         └───────────┬────────────────────┘                       │
│                     ▼                                            │
│            Server_DataReplay (Facade)                            │
│          统一后端接口，封装所有内部模块                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 函数调用 + Qt信号/槽
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Server_DataReplay (dll)                        │
│                      后端 — 核心业务引擎                         │
│                                                                  │
│  职责: 所有业务逻辑 + 数据处理 + NATS通信                          │
│  依赖: libnats.dll (NATS细节完全封装在内部)                       │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              业务逻辑层                                    │   │
│  │  ┌────────────┐  ┌──────────────┐  ┌───────────┐        │   │
│  │  │ ScenarioMgr │  │ ReplayEngine │  │LogService │        │   │
│  │  │ 想定管理    │  │ 回放引擎     │  │ 日志服务  │        │   │
│  │  └────────────┘  └──────┬───────┘  └───────────┘        │   │
│  │                          │                                │   │
│  │  ┌───────────────────────┴──────────────────────────┐    │   │
│  │  │              数据访问层 + 通信层                   │    │   │
│  │  │  ┌──────────────┐  ┌────────────────────────┐    │    │   │
│  │  │  │ DataFileReader│  │ Communication_NATS    │    │    │   │
│  │  │  │ 大文件游标读取 │  │ (单例 发布/订阅/重连)   │    │    │   │
│  │  │  └──────────────┘  └──────────┬─────────────┘    │    │   │
│  │  │                               │ 内部使用          │    │   │
│  │  │  ┌────────────────────────────┴──────────────┐   │    │   │
│  │  │  │            NATSClient                     │   │    │   │
│  │  │  │      nats.c C库的Qt面向对象封装             │   │    │   │
│  │  │  └───────────────────────────────────────────┘   │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Server_DataReplay (Facade) 提供统一信号:                         │
│    stateChanged / simTimeChanged / progressChanged               │
│    replayFinished / errorOccurred / natsConnected / newLog       │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 模块依赖关系（Facade 模式）

```
APP_DataReplay (exe)
  ├── 链接 Server_DataReplay.dll
  └── include 仅 "Server_DataReplay.h"    ← 不感知内部模块

Server_DataReplay (dll)
  ├── Server_DataReplay (Facade)          ← 统一入口，封装所有内部调用
  │   ├── ScenarioMgr                     ← 想定XML解析、目录扫描、映射配置
  │   ├── ReplayEngine                    ← 回放状态机、步进、倍速
  │   │   ├── DataFileReader (多实例)     ← 大文件游标式读取
  │   │   └── Communication_NATS          ← NATS消息发送
  │   ├── LogService                      ← 日志收集(信号+文件，完全内聚在Facade内)
  │   └── Communication_NATS              ← NATS断开时自动暂停回放
  └── 第三方依赖
      └── 链接 libnats.dll                ← NATS细节完全封装在内部
```

## 三、模块详细设计

### 3.1 ScenarioMgr — 想定管理

**目录自动关联规则**：
```
dataFiles/
└── 想定-海上编队巡逻想定/       ← 文件夹包含"想定-"前缀
    ├── 想定.xml                ← 自动识别为想定文件
    └── 回放数据/                ← 约定子目录名
        ├── data1.txt           ← 自动关联为该想定的数据文件
        └── data2.txt
```

**数据结构**：
```cpp
struct EntityInfo {
    QString id;
    QString name;
};

struct ScenarioSummary {
    QString name;
    QString scenarioDir;      // 想定文件夹路径
    QString filePath;         // 想定.xml 路径
    int entityCount;
};

struct Scenario {
    QString name;
    QString filePath;
    QDateTime startTime;
    QDateTime endTime;
    int simStepMs;
    QStringList dataFiles;    // 关联的数据文件路径列表
    QList<EntityInfo> entities;
};

struct DataFileInfo {
    QDateTime minTime;        // 最早仿真时间（回放引擎进度计算和结束判断用）
    QDateTime maxTime;        // 最晚仿真时间
};
```

**接口设计**：
```cpp
class ScenarioMgr : public QObject {
    Q_OBJECT
public:
    // 浅解析扫描 dataFiles/ 目录，返回所有想定摘要列表（快速）
    QList<ScenarioSummary> scanScenarios();

    // 完整解析指定想定（XML + 关联数据文件）
    bool loadScenario(const QString& scenarioFilePath);

    // 获取当前想定完整信息
    const Scenario* currentScenario() const;

signals:
    void scenarioLoaded(const Scenario& scenario);
    void scenarioUnloaded();
};
```

**浅解析优化**：`scanScenarios()` 调用 `parseScenarioXml(shallow=true)` 仅解析想定元信息和实体数量，跳过 Entity 子元素细节，大幅加快扫描速度。

### 3.2 DataFileReader — 大文件数据读取

**核心设计**：游标式顺序读取，不加载全文到内存。

**数据结构**：
```cpp
struct DataRecord {
    QDateTime timestamp;        // 数据时间戳
    QString rawLine;            // 原始行内容
};
```

**接口设计**：
```cpp
class DataFileReader : public QObject {
    Q_OBJECT
public:
    bool openFile(const QString& filePath);
    void close();

    // 预扫描信息
    DataFileInfo fileInfo() const;

    // 获取指定时间窗口 [windowStart, windowStart + stepMs) 内的数据
    QList<DataRecord> readWindow(const QDateTime& windowStart, int stepMs);

    // 重置到文件开头
    void reset();

signals:
    void progressChanged(double percent);
};
```

**执行流程**：
```
文件打开时:
  └─ 只读取:
      ├─ 第1行 → 解析起始仿真时间
      └─ 尾部 4KB 缓冲区 → 提取最晚仿真时间

回放读取时 (readWindow):
  └─ 维护 QFile + qint64 游标位置
  └─ 每次调用:
       ├─ 从游标开始顺序读行
       ├─ 解析每行时间戳
       ├─ 时间戳 < 窗口结束 → 加入结果列表
       ├─ 时间戳 ≥ 窗口结束 → 停止，记录游标偏移量
       └─ 返回窗口内所有数据
```

### 3.3 ReplayEngine — 回放引擎

**状态机**：
```
         ┌───────────────────────────────────────┐
         │                                       │
         ▼                                       │
    ┌──────────┐  开始   ┌──────────┐  暂停   ┌───────┐
    │ 未初始化 │───────→ │ 回放中   │───────→│ 暂停  │
    │ (Idle)  │         │ (Playing)│←───────│(Paused)│
    └────┬─────┘         └────┬─────┘  继续  └───┬───┘
         │                    │                   │
         │ 初始化             │ 停止              │ 停止
         ▼                    ▼                   ▼
    ┌──────────┐         ┌──────────┐
    │ 已就绪   │         │ 已停止   │
    │ (Ready)  │         │ (Stopped)│
    └──────────┘         └──────────┘
```

**接口设计**：
```cpp
class ReplayEngine : public QObject {
    Q_OBJECT
public:
    enum State { Idle, Ready, Playing, Paused, Stopped };

    // 初始化（传入已加载的想定）
    bool initialize(Scenario* scenario);

    bool start();     // 开始回放
    bool pause();     // 暂停
    bool resume();    // 继续
    bool stop();      // 停止（回到Ready）

    void setSpeed(int speed);  // 1~100，运行中实时生效
    int speed() const;

    State state() const;
    QDateTime currentSimTime() const;
    double overallProgress() const;

signals:
    void stateChanged(State newState);
    void simTimeChanged(const QDateTime& simTime);
    void progressChanged(double percent);
    void replayFinished();
    void errorOccurred(const QString& error);
};
```

**步进逻辑**：
```
物理定时器间隔 = 仿真步长(ms) / 倍速

例: 步长=100ms, 2倍速 → 定时器间隔=50ms
    步长=100ms, 0.5倍速 → 定时器间隔=200ms

每次 tick():
  1. 计算窗口 [currentTime, currentTime + stepMs)
  2. 从所有 DataFileReader 读取窗口数据
  3. 按时间戳全局排序（跨文件有序）
  4. 逐条调用 Communication_NATS::sendMsgData() 发送
  5. currentTime += stepMs
  6. currentTime > endTime → 停止并发射 replayFinished
  7. 发射 simTimeChanged / progressChanged
```

### 3.4 Communication_NATS — NATS通信（现有资产）

直接复用已有的 `Communication_NATS` 单例。通过其 `sendMsgData()` 接口发送回放数据。

**配置文件中新增回放主题配置项**：
```xml
<Root>
    <AttrIP IP="127.0.0.1" />
    <AttrTOPIC TOPIC="NATSMSG" />
    <AttrTOPIC TOPIC="SendToB" />
    <AttrTOPIC TOPIC="SendToA" />
    <AttrTOPIC TOPIC="DataReplay" />     <!-- 新增：回放数据发送主题 -->
</Root>
```

### 3.5 LogService — 日志服务（完全内聚在 Facade 内部）

LogService 完全由 `Server_DataReplay` 内部托管，APP 层通过 Facade 的 `log()` 方法和 `newLog` 信号间接使用，不直接依赖 LogService。

```cpp
class LogService : public QObject {
    Q_OBJECT
public:
    static LogService& instance();

    void log(const QString& level, const QString& message);
    void setLogFile(const QString& dirPath);

signals:
    void newLog(const QString& formattedMessage);  // 经 Facade 转发给 APP

private:
    QFile*        m_logFile;
    QTextStream*  m_logStream;  // 复用，避免每次写入重新构造
    mutable QMutex m_mutex;     // mutable 允许 const 方法中加锁
};
```

**特性**：
- 构造函数中不提前创建日志文件，延迟到 `Server_DataReplay::initialize()` 调用时才创建，避免孤儿文件
- `QTextStream` 复用为成员变量，高频日志场景下减少对象构造开销
- `logFilePath()` 通过 `mutable QMutex` 保证线程安全
- 日志信号通过 Facade 的 `newLog` 信号统一转发给 APP

**日志格式**：
```
[2026-07-11 14:32:05] [INFO] 想定加载成功 - 海上编队巡逻想定
[2026-07-11 14:32:06] [INFO] NATS连接成功 (127.0.0.1:4222)
[2026-07-11 14:32:07] [INFO] 回放开始
[2026-07-11 14:32:08] [WARN] 数据文件读取到末尾
```

## 四、UI界面设计

### 4.1 布局结构

```
┌─────────────────────────────────────────────────────────────┐
│                     数据回放软件                              │
├────────────────────────┬────────────────────────────────────┤
│  📁 文件管理            │  ⚙ 实体配置                     │
│  ┌──────────────────┐  │  ┌──────────────────────────┐   │
│  │ ▼ 海上编队巡逻想定 │  │  │ ID  │ 名称  │ 类型  │ 状态│   │
│  │  步长:100ms       │  │  │2001 │CA-001 │ 客机  │ 待命│   │
│  │  07-01~07-02      │  │  │2002 │CA-002 │ 客机  │ 待命│   │
│  │  实体:21个        │  │  │ ...                      │   │
│  │  ├─ ☑ 回放数据1   │  │  └──────────────────────────┘   │
│  │  │   (120MB)      │  │                                  │
│  │  └─ ☑ 回放数据2   │  │  ┌──────────────────────────────┐│
│  │      (85MB)       │  │  │ 🎮 导调控制                  ││
│  │                    │  │  │ [初始化] [开始] [暂停]       ││
│  │ [加载想定]        │  │  │ [继续]  [停止]               ││
│  └──────────────────┘  │  │ 倍速:[___]x  范围:1-100      ││
│                        │  │                              ││
│                        │  │ ⏱ 当前: 2025-07-01 14:32:05 ││
│                        │  │ ████████░░░░░░░░░░ 45%      ││
│                        │  └──────────────────────────────┘│
│                        │  ┌──────────────────────────────┐│
│                        │  │ 📋 日志信息                  ││
│                        │  │ [14:32:05] 想定加载成功      ││
│                        │  │ [14:32:06] NATS已连接        ││
│                        │  │ [14:32:07] 开始回放...       ││
│                        │  └──────────────────────────────┘│
├────────────────────────┴────────────────────────────────────┤
│  状态栏: NATS已连接 | 想定:海上编队巡逻 | 进度:45%         │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 控件清单

| 区域 | 控件 | 用途 |
|------|------|------|
| 文件管理 | QTreeView + QStandardItemModel | 树形展示想定和数据文件 |
| 文件管理 | QPushButton "加载想定" | 触发想定加载 |
| 实体配置 | QTableView + QStandardItemModel | 显示当前想定下的实体列表 |
| 导调控制 | QPushButton x5 | 初始化/开始/暂停/继续/停止 |
| 倍速 | QLineEdit + QIntValidator(1,100) | 手动输入倍速值 |
| 进度 | QLabel (当前仿真时间) | 显示当前仿真时间 |
| 进度 | QProgressBar | 显示整体进度百分比 |
| 日志 | QTextEdit (只读模式) | 显示带时间戳的日志信息 |
| 状态栏 | QStatusBar | 显示NATS/想定/进度状态 |

### 4.3 按钮状态联动

```
         Idle    Ready   Playing  Paused  Stopped
初始化    ✓       ✓       ✗       ✗       ✓
开始      ✗       ✓       ✗       ✗       ✗
暂停      ✗       ✗       ✓       ✗       ✗
继续      ✗       ✗       ✗       ✓       ✗
停止      ✗       ✓       ✓       ✓       ✗
倍速      ✓       ✓       ✓       ✓       ✓
```

## 五、前后端交互流程（通过 Facade）

```
用户操作 → APP前端 → Server_DataReplay (Facade) → 内部模块
───────────────────────────────────────────────────────

[启动程序]
  APP → m_server->initialize(logDir)
         ├── LogService::setLogFile()     ← 内部
         └── Communication_NATS::initNATSConnect()
  APP → m_server->scanScenarios()
     ← 返回想定摘要列表
  APP → 显示在树形列表中

[选择/加载想定]
  APP → m_server->loadScenario(filePath)
         └── ScenarioMgr::loadScenario()  ← 内部
     ← 返回完整想定信息
  APP → 树形列表展开 + populateEntityTable()

[初始化]
  APP → m_server->initReplay(scenario, files)
         ├── ReplayEngine::initialize()   ← 内部
         │   ├── 创建DataFileReader
         │   └── 扫描数据文件时间范围
         └── 日志记录
     ← 状态变为 Ready

[开始]
  APP → m_server->startReplay()
         └── ReplayEngine::start()
              └── QTimer 启动
                   └── 每次 tick():
                        ├── readWindow()
                        ├── 跨文件排序
                        ├── 实体ID映射替换
                        ├── NATS发送
                        └── 信号: simTimeChanged / progressChanged

[NATS断开]
  Facade 自动检测 natsConnected(false)
    ├── 引擎处于 Playing? → 自动暂停回放
    └── 发射 natsConnected(false) → APP 更新状态栏

[暂停/继续/停止]
  APP → m_server->pauseReplay() / resumeReplay() / stopReplay()
     ← 状态变化信号

[日志]
  APP → m_server->log(level, msg) → 内部写入文件 + 发射 newLog 信号
  APP ← 连接 m_server->newLog → appendLog() 显示
```

## 六、项目配置变更

### 6.1 APP_DataReplay.pro 修改

```pro
QT       += core gui
greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
CONFIG += c++11

DESTDIR = $$PWD/../../bin/

# 添加 Server 库的头文件路径
INCLUDEPATH += $$PWD/../../include

SOURCES += \
    main.cpp \
    DataReplayWidget.cpp

HEADERS += \
    DataReplayWidget.h

FORMS += \
    DataReplayWidget.ui

# 链接 Server_DataReplay 库
LIBS += -L$$PWD/../../bin -lServer_DataReplay

# Default rules for deployment.
qnx: target.path = /tmp/$${TARGET}/bin
else: unix:!android: target.path = /opt/$${TARGET}/bin
!isEmpty(target.path): INSTALLS += target
```

### 6.2 Server_DataReplay.pro 修改

```pro
QT -= gui
TEMPLATE = lib
DEFINES += SERVER_DATAREPLAY_LIBRARY
CONFIG += c++11

INCLUDEPATH += $$PWD/../../include

SOURCES += \
    Communication/Communication_Interior.cpp \
    Communication/Communication_NATS.cpp \
    Communication/NATSClient.cpp \
    Server_DataReplay.cpp \
    ScenarioMgr.cpp \          # 新增
    DataFileReader.cpp \       # 新增
    ReplayEngine.cpp \         # 新增
    LogService.cpp             # 新增

HEADERS += \
    Communication/Communication_Interior.h \
    Communication/Communication_NATS.h \
    Communication/NATSClient.h \
    Server_DataReplay_global.h \
    Server_DataReplay.h \
    ScenarioMgr.h \            # 新增
    DataFileReader.h \         # 新增
    ReplayEngine.h \           # 新增
    LogService.h               # 新增

LIBS += -L$$PWD/../../bin -lnats

# Default rules for deployment.
unix {
    target.path = /usr/lib
}
!isEmpty(target.path): INSTALLS += target
```

### 6.3 config/NATS/NatsConfig.xml 修改

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Root>
    <AttrIP IP="127.0.0.1" />
    <AttrTOPIC TOPIC="NATSMSG" />
    <AttrTOPIC TOPIC="SendToB" />
    <AttrTOPIC TOPIC="SendToA" />
    <AttrTOPIC TOPIC="DataReplay" />     <!-- 新增：回放数据发送主题 -->
</Root>
```

## 七、文件清单

### Server_DataReplay 层（Facade + 内部模块）

| 文件 | 说明 | 关键类/方法 |
|------|------|-------------|
| `Server_DataReplay.h` / `Server_DataReplay.cpp` | Facade 统一入口 | initReplay(), startReplay(), loadScenario（）, log()，信号转发 |
| `ScenarioMgr.h` / `ScenarioMgr.cpp` | 想定管理 | scanScenarios()（浅解析）, loadScenario(), saveEntityIdMapping() |
| `DataFileReader.h` / `DataFileReader.cpp` | 大文件读取 | openFile(), readWindow() |
| `ReplayEngine.h` / `ReplayEngine.cpp` | 回放引擎 | initialize(), start(), pause(), resume(), stop(), setSpeed() |
| `LogService.h` / `LogService.cpp` | 日志服务 | log(), setLogFile(), newLog 信号（完全内聚在 Facade 内） |

### APP_DataReplay 层

| 文件 | 变更 | 说明 |
|------|------|------|
| `DataReplayWidget.ui` | 修改 | 左侧 QTableWidget → QTreeView |
| `DataReplayWidget.h` | 修改 | m_scenarioMgr + m_replayEngine → m_server (Server_DataReplay*) |
| `DataReplayWidget.cpp` | 修改 | 所有后端调用通过 m_server-> 统一入口，移除所有内部模块 include |
| `ScenarioFilterProxyModel.h` / `ScenarioFilterProxyModel.cpp` | 新增 | 文件树递归过滤代理模型（QRegularExpression） |

## 八、未来功能扩展预留

### 8.1 网络接收推演数据

利用现有的 `Communication_NATS::messageReceived` 信号：

```
Communication_NATS::messageReceived
  └─ 新增 NetworkRecorder 模块:
       ├─ 连接 messageReceived 信号
       ├─ 数据写入 dataFiles/想定-XXX/回放数据/recv_YYY.txt
       └─ 写入后通知 ScenarioMgr 刷新文件列表
```

### 8.2 值替换后发送

在 `ReplayEngine::tick()` 中插入替换层：

```
读取数据 → ValueReplacer::apply(records) → 替换后 → NATS发送

ValueReplacer 规则:
  struct ReplaceRule {
      QString entityId;      // 目标实体ID
      QString fieldPath;     // 字段路径 "data.x"
      QString originalValue;
      QString newValue;
  };
```

### 8.3 日志增强

LogService 已预留信号+文件双输出通道：
- 日志级别筛选
- 日志搜索
- 按日期自动切割文件

### 8.4 导入想定功能

**需求**：从本地磁盘目录导入想定（XML + 数据文件），自动关联层级关系。

**设计决策**：

| 维度 | 选择 |
|------|------|
| 导入来源 | 本地磁盘目录（通过 QFileDialog 选择 XML） |
| 导入粒度 | 一次导入一个想定 |
| 同名冲突 | 自动覆盖（先删后写） |
| 数据文件关联 | 自动发现 + 用户可增减 |
| 目录命名 | 从 XML 中解析 `SceName` 作为目标目录名 |

**数据文件发现规则**：

```
源目录/
├── 想定.xml              ← 用户选择的 XML 文件
├── 回放数据/              ← 优先扫描（约定子目录）
│   ├── data1.json
│   └── data2.json
└── *.json                ← 兜底扫描（源目录下的 JSON）
```

**执行流程**：

```
用户点击"导入想定"
  │
  ▼
① QFileDialog 选择 XML 文件（filter: *.xml）
  │
  ▼
② ScenarioMgr::previewImport(xmlSourcePath)
     ├── parseScenarioXml() → sceName, entityCount
     ├── discoverDataFiles() 扫描源目录
     └── 返回 ImportPreview { sceName, entityCount, autoDiscoveredFiles, targetExists }
  │
  ▼
③ 弹出 ImportDialog 确认框
  ┌─────────────────────────────────────────┐
  │ 导入想定                                │
  │                                         │
  │ 想定名称：{sceName}                      │
  │ 实体数量：{entityCount}                   │
  │ 目标路径：dataFiles/{sceName}/           │
  │                                         │
  │ 数据文件列表：                           │
  │ ┌─────────────────────────────────────┐ │
  │ │ ☑ data1.json    1.2 MB  ✓ 自动发现 │ │
  │ │ ☑ data2.json    3.1 MB  ✓ 自动发现 │ │
  │ │ ☑ extra.json    0.5 MB      用户添加│ │
  │ └─────────────────────────────────────┘ │
  │ [添加文件]  ← QFileDialog::getOpenFileNames │
  │                                         │
  │ ⚠ 目标已存在，将覆盖旧文件（同名时显示）  │
  │                                         │
  │ [取消]  [确认导入]                        │
  └─────────────────────────────────────────┘
  │
  ▼
④ ScenarioMgr::importScenario(xmlSourcePath, sceName, selectedFiles)
     ├── QDir(dataFilesRoot + "/" + sceName) 创建目录
     ├── 若目标已存在 → removeRecursively()
     ├── 复制 XML → dataFiles/{sceName}/
     ├── 创建 {sceName}/回放数据/
     └── 复制勾选的 JSON → {sceName}/回放数据/
  │
  ▼
⑤ refreshScenarioTree() 刷新界面
```

**确认对话框交互规则**：

| 来源 | 标记 | 可勾选 | 可移除 |
|------|------|--------|--------|
| 自动发现 | 绿色 ✓ | 是 | 否 |
| 用户添加 | 无标记 | 是 | 是（右键 / Delete 键） |

- 默认全选所有文件
- 不允许空选（取消全部勾选时确认按钮置灰）
- 用户添加时自动过滤已存在的文件（去重）

**新增数据结构**：

```cpp
// 导入预览信息（ScenarioMgr 返回给 APP 层）
struct ImportPreview {
    QString sceName;                  // XML 解析出的想定名称（SceName）
    int entityCount;                  // 实体数量
    QStringList autoDiscoveredFiles;  // 自动发现的 JSON 文件绝对路径
    QString xmlSourcePath;            // 源 XML 路径
    QString sourceDir;                // 源目录路径
    bool targetExists;                // 目标 dataFiles/{sceName}/ 是否已存在
};
```

**新增接口**：

```cpp
// ScenarioMgr 新增方法
class ScenarioMgr : public QObject {
public:
    // 预览导入：解析 XML + 扫描源目录，返回预览信息（不执行实际复制）
    ImportPreview previewImport(const QString &xmlSourcePath);

    // 执行导入：创建目录、复制文件
    bool importScenario(const QString &xmlSourcePath,
                        const QString &sceName,
                        const QStringList &selectedFiles);

private:
    // 扫描源目录，自动发现数据文件（回放数据/ 优先，根目录兜底）
    QStringList discoverDataFiles(const QString &sourceDir);
};

// Server_DataReplay (Facade) 新增转发方法
class Server_DataReplay : public QObject {
public:
    ImportPreview previewImport(const QString &xmlSourcePath);
    bool importScenario(const QString &xmlSourcePath,
                        const QString &sceName,
                        const QStringList &selectedFiles);
};

// DataReplayWidget 新增 slot
class DataReplayWidget : public QWidget {
private slots:
    void onImportScenario();  // 导入想定的完整 UI 流程入口
};
```

**涉及文件变更**：

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `DataReplayWidget.h` | 修改 | 新增 `onImportScenario()` slot |
| `DataReplayWidget.cpp` | 修改 | 实现：文件选择 → 调用 Facade → ImportDialog → 执行导入 |
| `Server_DataReplay.h` | 修改 | 新增 `previewImport()` + `importScenario()` |
| `Server_DataReplay.cpp` | 修改 | Facade 转发到 ScenarioMgr |
| `ScenarioMgr.h` | 修改 | 新增 `previewImport()` + `importScenario()` + `discoverDataFiles()` + `ImportPreview` 结构体 |
| `ScenarioMgr.cpp` | 修改 | 实现文件扫描 + 目录创建 + 文件复制 |
| `DataReplayWidget.ui` | 修改 | 工具栏/菜单栏新增"导入想定"按钮 |

### 8.5 导入后追加数据文件

**需求**：导入想定后（或想定已存在时），用户可能需要向想定目录追加新的数据文件。

**两个入口**：

#### 入口一：右键菜单「添加数据文件」

```
文件树中右键想定文件夹节点
  │
  ▼
弹出 QFileDialog::getOpenFileNames（筛选 *.json *.txt）
  │
  ▼
ScenarioMgr::addDataFiles(scenarioDir, selectedPaths)
  ├── 确保 {scenarioDir}/回放数据/ 目录存在（不存在则创建）
  ├── 遍历选中文件，复制到 回放数据/ 下
  │   └── 同名文件 → 覆盖（QFile::copy 前先 remove）
  └── 返回成功复制的文件数量
  │
  ▼
refreshScenarioTree() 刷新界面
```

#### 入口二：右键菜单「重新扫描」

```
文件树中右键想定文件夹节点
  │
  ▼
ScenarioMgr::refreshDataFiles(scenarioDir)
  ├── 扫描 {scenarioDir}/回放数据/ 下的所有 *.json
  └── 返回当前数据文件绝对路径列表
  │
  ▼
refreshScenarioTree() 刷新界面（与树节点同步）
```

**适用场景**：

| 场景 | 使用入口 |
|------|----------|
| 导入时忘记选数据文件，事后从其他目录添加 | 添加数据文件 |
| 用户手动拖放/复制文件到想定目录 | 重新扫描 |
| 外部程序写入了新的数据文件 | 重新扫描 |
| 数据文件被外部程序删除，需同步树节点 | 重新扫描 |

**右键菜单结构（更新后）**：

```
想定文件夹节点右键:
  ├── 添加数据文件...     ← 新增
  ├── 重新扫描            ← 新增
  ├── ─────────────
  ├── 重命名
  └── 删除
```

**新增接口**：

```cpp
// ScenarioMgr 新增方法
class ScenarioMgr : public QObject {
public:
    // 向已有想定追加数据文件（复制到回放数据/ 目录）
    // 返回成功复制的文件数量，-1 表示全部失败
    int addDataFiles(const QString &scenarioDir, const QStringList &filePaths);

    // 重新扫描想定的数据文件目录，返回当前所有数据文件路径
    QStringList refreshDataFiles(const QString &scenarioDir);
};

// Server_DataReplay (Facade) 新增转发方法
class Server_DataReplay : public QObject {
public:
    int addDataFiles(const QString &scenarioDir, const QStringList &filePaths);
    QStringList refreshDataFiles(const QString &scenarioDir);
};

// DataReplayWidget 新增 slot
class DataReplayWidget : public QWidget {
private slots:
    void onAddDataFiles(const QModelIndex &sourceIndex);
    void onRefreshDataFiles(const QModelIndex &sourceIndex);
};
```

**涉及文件变更**：

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `ScenarioMgr.h` | 修改 | 新增 `addDataFiles()` + `refreshDataFiles()` |
| `ScenarioMgr.cpp` | 修改 | 实现文件复制 + 目录扫描 |
| `Server_DataReplay.h` | 修改 | 新增 Facade 转发声明 |
| `Server_DataReplay.cpp` | 修改 | Facade 转发实现 |
| `DataReplayWidget.h` | 修改 | 新增 `onAddDataFiles()` + `onRefreshDataFiles()` slot |
| `DataReplayWidget.cpp` | 修改 | 右键菜单增加两项 + 槽函数实现 |

## 九、开发阶段划分

| 阶段 | 内容 | 涉及文件 |
|------|------|----------|
| 1. 项目配置调整 | APP链接Server库 + Server添加新模块源文件 | APP_DataReplay.pro, Server_DataReplay.pro |
| 2. 数据结构定义 | Scenario/EntityInfo/DataRecord/DataFileInfo | ScenarioMgr.h (新) |
| 3. ScenarioMgr | 想定XML解析 + 目录扫描 + 文件关联 | ScenarioMgr.h/cpp (新) |
| 4. DataFileReader | 大文件游标式读取 + 时间窗口切片 | DataFileReader.h/cpp (新) |
| 5. ReplayEngine | 回放状态机 + 定时器步进 + 倍速 + NATS发送 | ReplayEngine.h/cpp (新) |
| 6. LogService | 日志收集 + 信号发射 + 文件写入 | LogService.h/cpp (新) |
| 7. UI改造 | 树形列表 + 按钮连接 + 进度条 + 日志对接 | DataReplayWidget.全部 |
| 8. 集成联调 | 全流程测试、边界情况处理 | 所有文件 |
